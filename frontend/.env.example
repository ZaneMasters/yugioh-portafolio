# URL del backend (si usas el proxy de Vite, déjalo vacío)
# VITE_API_URL=http://localhost:3000/api/v1

# Con el proxy de vite.config.js, las peticiones a /api/* se redirigen automáticamente
# al backend en localhost:3000 durante desarrollo
VITE_API_URL=/api/v1
